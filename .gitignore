# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local secrets (Meshy API key etc.) — keep .env.example tracked
.env

# Playwright test artifacts
test-results
playwright-report

# Scratch / debug screenshots + throwaway specs
tmp-*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Blender autosaves and hand-made backups — the .blend beside them is the source
*.blend1
.dev/blender/*.pre-*.blend

# Machine-local tool config: absolute paths and a per-machine MCP port.
# The port registry that governs it lives outside the repo, in
# Documents/App/.claude/MCP-PORTS.md
.mcp.json
.claude/settings.local.json
